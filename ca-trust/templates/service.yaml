apiVersion: v1
kind: Service
metadata:
  name: {{ include "ca-trust.fullname" . }}
  labels:
    {{- include "ca-trust.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ca-trust.selectorLabels" . | nindent 4 }}
